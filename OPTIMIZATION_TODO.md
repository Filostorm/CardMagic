# CardMagic Optimization Todo

Deployment note: beta Cloudflare deployment is intentionally paused during this optimization run. Do not run `npm run deploy:cloudflare` until the pause is lifted.

## Active Queue

- [x] P0: Make batch export use bounded-memory output streaming where the platform supports file streams.
- [x] P0: Move account set replacement toward atomic, diff-oriented persistence instead of client-side replace-all sync.
- [x] P1: Redesign the web media store around owner references and indexed pruning.
- [x] P1: Bound remote media upload/materialization caches so large data URIs are not retained indefinitely.
- [x] P1: Continue `App.tsx` decomposition into domain hooks and infrastructure modules.
- [x] P0: Centralize the active-card state machine behind a typed `useReducer`.
- [~] P1: Push transient UI state out of `App()` into the leaf components that own it (started: `newSetName`).
- [x] P1: Move the floating-toolbar drag off the React render path (reanimated shared value; commit on release).
- [x] P1: Extract pure image-codec and subject-mask CV helpers out of `App.tsx` into `src/lib`.
- [x] P1: Extract the community feature (panel + 14 sub-components) out of `App.tsx` into `src/components/community-panel.tsx`.
- [x] P1: Extract the art-source + AI-generator modals out of `App.tsx` into `src/components/art-generator-modals.tsx`.
- [x] P0: Stand up a unit-test harness (Vitest) and seed the first suite for the extracted pure modules.
- [x] P1: Extract `cardEditorReducer` and the semver helpers into testable `src/lib` modules and cover them with tests.
- [x] P1: Reduce Oracle autocomplete payload and parse cost.
- [x] P1: Split broad frame asset manifests into lazy treatment/typeframe families.
- [x] P2: Decompose `CardPreview` into model generation plus variant renderers.
- [x] P1: Memoize pure frame-bitmap leaf layers so card-text edits skip frame re-render.
- [x] P2: Virtualize unbounded dynamic lists.
- [x] P2: Remove duplicate/dead source and enable unused-symbol hygiene.
- [x] P2: Add consistent provider timeouts/idempotency around AI edge flows.
- [x] P3: Remove string-built checkout popup DOM.

## Completed In This Run

- Added a deployment-pause note so beta Cloudflare deploy is intentionally skipped while optimization work is active.
- Replaced the active card's `useState` plus 14 scattered `setCard` mutation sites with a single typed `cardEditorReducer`/`useReducer`, so every card transition (patch, replace, randomize, reset, next-blank, flip-side convert/remove, default-credit, merge-defaults) is a named, greppable action; surrounding dirty-flag and sibling UI state were left untouched for a behavior-preserving change verified with `npm exec tsc -- --noEmit`.
- Wrapped the pure frame-bitmap leaf layers in `card-preview.tsx` (`StableFrameImage`, `DirectFrameImage`, `MseFrameImage`, `MseSplitFrameImage`, `ShowcaseFrameImage`, `ShowcaseFrameBlendImage`, `MseTextureLayer`, `FutureFrameBlendTextureLayer`, `FutureFrameMulticolorUnderlay`, `TextlessBottomBarLayer`) in `React.memo`. These render purely from `(source, scalar, cacheKey)` props and never read the mutating `card` object, so they now bail out of re-render when only card text changes — the editor's highest-frequency update. Verified with `npm exec tsc -- --noEmit` and `npm run export:web`.
- Deleted the unimported duplicate `src/components/card-preview 2.tsx` source file.
- Deleted the fully dead `src/data/basic-m15-frame-gallery.ts` (533 KB / 12.5K lines); confirmed none of its four exported symbols are referenced anywhere, then re-verified with `npm exec tsc -- --noEmit` and `npm run export:web`.
- Re-enabled `noUnusedLocals`/`noUnusedParameters` in `tsconfig.json` and cleared the two findings it surfaced: removed the dead `saveCurrentCardToSelectedSet` wrapper and removed the write-only `subjectMaskDiagnostics` state plus its five setter calls (diagnostics still reach the trace via `pushSubjectMaskDiagnosticsTrace`, so behavior is unchanged while needless masking-time re-renders are eliminated). Verified with `npm exec tsc -- --noEmit` and `npm run export:web`.
- Gated the six pure-diagnostic `console.log`/`console.info` calls (subject-mask status/trace in `App.tsx`, `logStorageInfo` in `App.tsx` and `src/lib/web-media-store.ts`) behind `__DEV__` so Metro strips them from the production web bundle, while leaving the 115 error-path `console.warn` calls intact as real failure breadcrumbs. Verified with `npm exec tsc -- --noEmit` and `npm run export:web`.
- Began `App.tsx` infrastructure decomposition (#5): extracted six pure image-codec helpers into `src/lib/image-codec.ts` and the twelve pure subject-mask computer-vision helpers (plus their 5 types and 3 request-dimension constants, with a private `clamp` to avoid any import back into `App.tsx`) into `src/lib/subject-mask.ts`. App.tsx dropped from 24,042 to 23,306 lines. All moves are verbatim/behavior-preserving; verified with `npm exec tsc -- --noEmit`, `npm run export:web`, and a live web smoke test (app loads and renders identically). Local dev server config added at `.claude/launch.json`.
- Continued #5: moved `createPendingStripeCheckoutPopup` (the web Stripe-checkout popup DOM builder) from `App.tsx` into the existing `src/lib/commerce.ts` alongside the rest of the checkout logic (`Platform`/`window` already imported there). App.tsx down to 23,234 lines. Verified with `npm exec tsc -- --noEmit`, `npm run export:web`, and a live browser test: typed into the Card name field and confirmed the preview re-rendered through the new `useReducer` dispatch path end-to-end.
- Began pushing transient UI state into leaf components (second half of #1): moved the `newSetName` text-input state from `App()` down into `SetsPanel`, which now owns it locally and passes the trimmed name up through `onCreateSet(name)` on submit (App's `createSet` no longer reads/clears App state). Typing a new set name now re-renders only `SetsPanel` instead of the whole 6,000-line `App()`. App.tsx `useState` count 193 → 191. Verified with `npm exec tsc -- --noEmit`, `npm run export:web`, and a live end-to-end browser test (typed a name, created a set, saw it appear with the "First custom set" achievement, field cleared).
- Made the card state machine and version logic testable: extracted the semver helpers into `src/lib/semver.ts` and the `cardEditorReducer` (plus its pure closure — `getFrameGeometryArtResetPatch`, `withDefaultCardCredit` + credit helpers, `createStarterCard`/`createNextBlankCard`/`getNextCollectorNumber`, `DEFAULT_ART_TRANSFORM`) into `src/lib/card-editor-reducer.ts`, both self-contained (no App import). App.tsx dropped to 18,939 lines (~21% below the 24,042 session start). Added 19 more tests (7 semver, 12 reducer) for 43 total, including the reducer's frame-geometry art-reset behavior, referential-identity credit guard, and DFC flip transitions. Taught Vitest to stub static-asset `import`/`require` (the frame modules' PNG/font assets) so logic that transitively pulls them runs in plain node. Verified with `npm test` (43 pass), `npm exec tsc -- --noEmit`, `npm run export:web`, and a live browser edit (card title updated through the extracted reducer).
- Stood up the project's first automated tests: added Vitest (`vitest.config.ts` with a node environment and the `@/*` path alias), `npm test`/`npm run test:watch` scripts, and 24 unit tests across 4 files under `src/lib/__tests__/` covering the RN-free pure modules extracted this session — `uuid` (`createUuid`), `format-comment-date`, `image-codec` (data-URI parsing, mime/extension mapping, URI classification), and `subject-mask` geometry (`scaleSubjectMaskBoxPromptForRequest`, `scaleSubjectMaskPointPromptsForRequest`, `getPaddedSubjectMaskCropRegion`, `getRectDistance`, `groupRoughSelectionBrushSamples`, `createRoughSelectionBrushPath`). All 24 pass; `npm exec tsc -- --noEmit` and `npm run export:web` remain green (test files aren't in the app bundle graph). This is the regression net that de-risks the larger `App()` state decomposition. Follow-ups: extract `cardEditorReducer` + `compareSemanticVersions` to their own modules so they become testable, then cover the connected-component mask functions.
- Extracted the art-source and AI-generation modals (`ArtSourceModal`, `ArtLibraryThumbnail`, `ArtSourceMenuItem`, `ArtGeneratorModal`, `SetSymbolGeneratorModal`, `CardBackGeneratorModal`, ~1,040 lines) out of `App.tsx` into `src/components/art-generator-modals.tsx`. Lifted shared support into new modules both sides import: `src/types/art.ts` (`ArtLibrarySource`, `ArtLibraryEntry`, `CardBackGeneratorMode`, `SubjectMaskTraceEntry`) and `src/lib/art-image-quality.ts` (`ART_IMAGE_QUALITY_OPTIONS` + derived `ArtImageQuality` + `ART_LIBRARY_VISIBLE_THUMBNAIL_LIMIT`). The two art-library helpers (`getArtLibraryColorLabel`, `sortArtLibraryEntries`, one use each) are passed in as props. App.tsx dropped from 20,192 to 19,116 lines (~3,900 below the 24,042 session start). Verified with `npm exec tsc -- --noEmit`, `npm run export:web`, and a live browser test (opened the Art Source modal; its content rendered correctly).
- Extracted the entire community feature out of `App.tsx` into `src/components/community-panel.tsx` (~3,000 lines: `CommunityPanel` + 14 sub-components + 2 helpers + the community constants). To break the dependency cycle (a component module must not import the root entry file), first lifted the shared core into new modules both sides import: `src/types/app-domain.ts` (`CardSet`, `SetCardSnapshot`, `MainScrollWindow`, `MainScrollBoundaryHandler`, `PreviewTypeFrame`), `src/lib/uuid.ts` (`createUuid`), `src/lib/preview-type-frame.ts` (`getPreviewTypeFrame`), `src/lib/app-version.ts` (`CARDMAGIC_APP_VERSION`), and `src/lib/format-comment-date.ts`. The two deep-dependency helpers (`getSafeSetCardPreviewCard`, `normalizePickedImage`, one use each) are passed into `CommunityPanel` as props instead of moved. App.tsx dropped from 23,236 to 20,192 lines (~3,044 fewer; now ~16% smaller than the 24,042 session start). Verified with `npm exec tsc -- --noEmit`, `npm run export:web`, and a live browser test (Community tab renders the feed + Weekly Featured Card from the backend). Backup at /tmp/App.beforeCommunity.tsx.
- Moved the floating preview-toolbar drag off the React render path: the per-frame `runOnJS(updatePreviewToolbarDrag)` → `setPreviewToolbarPosition` (which re-rendered the entire ~6,000-line `App()` on every pointer frame) was replaced by a reanimated shared value (`previewToolbarDragOffset`) that drives the toolbar's animated `translate` entirely on the UI thread; `commitPreviewToolbarDrag(translationX, translationY)` resolves orientation docking + edge clamping and commits the final position to React state once, on release. `App()` now re-renders once per drag instead of ~60×/s. Behavior nuance: edge-clamp and orientation flip now resolve on release rather than live mid-drag. Verified with `npm exec tsc -- --noEmit`, `npm run export:web`, and live browser drag tests (dragged the toolbar to a new spot — committed correctly; dragged to the bottom edge — flipped to horizontal orientation on release; no errors).
- Bounded remote media materialization cache memory and removed full-data-URI upload cache keys.
- Added web save-handle ZIP streaming for batch exports.
- Added IndexedDB media owner-reference indexing and cursor-based pruning.
- Extracted the web media/store persistence subsystem from `App.tsx` into `src/lib/web-media-store.ts`.
- Added client-side AI edge timeouts, request IDs, and AI credit-spend idempotency references.
- Replaced string-built Stripe checkout placeholder DOM with explicit element construction.
- Changed account set membership sync from delete-all-then-upsert to upsert-current-then-delete-stale diffing.
- Split Oracle autocomplete onto a generated 3.7 MB tuple search index and deferred the 16 MB full card database until selection.
- Reconnected card JSON, set JSON, set batch export, and randomize actions through the header share menu.
- Removed low-risk unused imports, dead aliases, orphan helpers, and a duplicate MSE borderless treatment map.
- Removed dormant frame-template storage/import/export code and disconnected manual mask-paint brush code.
- Added strict unused-symbol verification (`noUnusedLocals`/`noUnusedParameters`) and cleared the remaining strict-unused clusters.
- Replaced the community card feed's unbounded progressive render with a measured scroll-window virtualizer.
- Split MSE M15 treatment and typeframe source maps into lazy initialized family modules.
- Extracted `CardPreview` frame/treatment/color derivation into `src/components/card-preview/card-preview-model.ts`.
- Optimized community feed hydration so rows with hosted rendered images keep compact card JSON and skip remote media materialization.
- Changed community seen-state writes from page-load eager RPCs to once-per-visible-row marking through the virtualized feed window.
- Verified with `npm exec tsc -- --noEmit`, Deno Edge Function checks, and `npm run export:web`.
